<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) && msg.length > 0 %>
    <script>

      $.notify("#{j(msg)}", {
        type: "#{(name == 'notice') ? 'success' : 'danger'}",
        newest_on_top: true,
        mouse_over: "pause",
      });

    </script>
  <% end %>
<% end %>
